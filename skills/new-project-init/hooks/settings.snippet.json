{
  "//": "対象プロジェクトの .claude/settings.json にこの hooks ブロックをマージする。block-main-push.sh は .claude/hooks/ に配置し、実行権限を付ける (chmod +x)。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/block-main-push.sh"
          }
        ]
      }
    ]
  }
}
